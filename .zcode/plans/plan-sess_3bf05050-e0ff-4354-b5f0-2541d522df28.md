# Whimread 仓库拆分：公开 Flutter 仓（历史重置）+ 私有后端仓

## 已确认决策
- 公开仓 `yunkst/Whimread`（已改名）：**重置 git 历史**为纯 Flutter 内容，旧 tag 删除、**Release 资产保留**（APP 更新链路依赖 /releases/latest）
- 新建私有仓 `yunkst/whimread-backend`：承载后端全部代码与部署编排
- 仓库已改名 Whimread，无需再改名；需更新代码/文档中全部旧名引用

## 操作序列（顺序即安全序：后端先落位，公开仓再重置）

### Phase A：私有后端仓（先做，后端先安全落位）
1. 在磁盘新建 `D:\myspace\whimread-backend`，复制以下内容：
   - `backend/**`（app/alembic/tests/scripts/pyproject/Dockerfile×2/mypy.ini/Makefile/alembic.ini/.env/.pre-commit）
   - 根 `docker-compose.yml` + `docker-compose.override.yml.example`（postgres+backend 编排归后端）
   - `.env.example`、`tests/api_examples/`（根级）、`backups/`、`database-updater/`、`scripts/novel_api_cli.py`
   - docs：`deployment.md`、`local-dev.md`（改写为单仓视角）、`developer-guide.md` 后端半
   - `.github/workflows/backend-ci.yml`（去 `backend/` 路径前缀与 paths 过滤）、`code-review.yml` 副本、`dependabot.yml`（只留 pip 节）
   - 根文档（AGENTS/CLAUDE）拆出的后端半 → 合并为私有仓的 AGENTS.md/CLAUDE.md
2. git init + 初始 commit + `gh repo create yunkst/whimread-backend --private` + push
3. 提醒用户手动配置私有仓 secrets（CODE_REVIEW 的 GH_TOKEN/LLM_*）

### Phase B：公开仓重置（D:\myspace\novel_builder 就地）
1. **保险**：`git bundle create ../novel_builder_backup.bundle --all`（本地留存完整旧历史，不推远端）
2. 内容重整（在磁盘直接做）：
   - 提升：`novel_app/**` → 根（lib/、android/、ios/、pubspec.yaml、assets…）
   - 移入：`tool/`（Flutter 测试脚本，内部路径修正）、`docs/{README(改),developer-guide(Flutter半),logging-guidelines,diagrams,mockups,plans,superpowers主体}`、`.claude/.agents` 的 novel-app-release skill（改 URL 为 Whimread）
   - workflows 路径修正：flutter-ci（删 working-directory/paths/artifact 前缀）、flutter-release（`../.github`→`.github`、APK/release_body 路径去前缀）、pages.yml 不变、dependabot 只留 pub+actions 节
   - 删除：backend/、database-updater/、根 tests/、backups/、根 requirements.txt、`scripts/{novel_api_cli,novel_crawler,smxku_crawler}.py`、后端 CI、`.env*`
   - 引用更新（旧名→Whimread）：`github_release_service.dart` `_repoName`、`native_crash_reporter.dart`、pubspec repository/homepage、docs-site index.html 约 15 处（APK 直链改 releases/latest）、release skill 脚本 8 处、.gitattributes/.gitignore 精简重建（去 Python/backend 段，路径去 novel_app/ 前缀）
   - 文档重写：README（Whimread 品牌+开发者节收缩）、根 AGENTS.md/CLAUDE.md（以 novel_app/CLAUDE.md 为主体重写，Flutter 专属）、CONTRIBUTING（去 Python 段）、docs/local-dev 拆出 APP 侧一节并入 README 或 docs
3. 历史重置：
   - `git checkout --orphan fresh-root` → add 全部新内容 → 单个初始 commit
   - `git branch -M fresh-root master` → `git push -f origin master`
   - 删除全部 78 个旧 tag（本地+远端；**Release 实体与 APK 资产保留**，/releases/latest 继续可用；tag 缺失的 release 在页面上正常显示资产）
4. GitHub 侧收尾：更新 repo 描述/homepage（Whimread）；确认默认分支 master；提醒用户旧 secrets 随改名保留、无需重配

### Phase C：验证
- 公开仓：`flutter analyze` + `flutter test test/unit/ test/bug/` 全绿；`flutter build apk --debug`（验证 gradle 相对路径无破坏）；push 后 flutter-ci 触发且绿
- 私有仓：pytest 全绿 + mypy/ruff 干净；backend-ci 触发且绿
- APP 更新链路：`gh api repos/yunkst/Whimread/releases/latest` 可达；`github_release_service` 指向新名
- docs-site 链接抽查无 404

## 明确不做
- 不迁移 git 旧历史到任何一侧（公开仓重置、私有仓全新）
- 不改 Dart 包名 `novel_app` 与 applicationId `com.example.novel_app`（22 处 import + 老用户升级通道）
- `.zcf/.zcode/.superpowers/.playwright-mcp/.mcp.json` 等本地 AI 工具杂物不入任何仓（gitignore/本地保留）
- Release/issue 不重建（自动保留）

## 风险与保险
- 回滚保险：`novel_builder_backup.bundle`（本地完整旧历史，不推远端）
- 旧 APK 的更新检查依赖 GitHub 重定向（novel_builder→Whimread），且新 APK 会带新名，无断链窗口
- docs-site 的 release 直链改为 /releases/latest 后不再写死版本号